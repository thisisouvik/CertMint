[package]
name = "verifier"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "22.0.1"
nft_certificate = { path = "../nft_certificate" }

[dev-dependencies]
soroban-sdk = { version = "22.0.1", features = ["testutils"] }
